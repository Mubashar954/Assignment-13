<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Assignment 13</title>
</head>
<body>
    <!-- /////////////////// Question No 01 /////////////////////// -->
    <!-- 1- How can we access HTML elements using the Document Object Model (DOM) in JavaScript? -->

    1. You can use the getElementById method to retrieve an element by its unique id attribute
    
        const element = document.getElementById('elementId');

    2. The getElementsByClassName method returns a collection of elements with the specified class name:

        const elements = document.getElementsByClassName('className');

    3.  This method allows you to obtain a collection of elements based on their tag name:

        const elements = document.getElementsByTagName('tagName');

    4.  The querySelector method allows you to select elements using CSS selectors:

        const element = document.querySelector('selector');

    5.  Similar to querySelector, querySelectorAll returns a collection of elements that match the specified CSS selector:

        const elements = document.querySelectorAll('selector');

    <!-- /////////////////// Question No 02 /////////////////////// -->
    <!-- What activities can be performed using the DOM? -->
    Using the Document Object Model (DOM), you can perform various activities to manipulate HTML elements and create dynamic web pages.
    . Accessing elements
    . Modifying element content
    . Changing element attributes
    . Manipulating CSS
    . Creating and deleting elements
    . Handling events
    . Traversing the DOM
    . Manipulating styles
    . Dynamic content generation
    . Form validation and submission
    <!-- /////////////////// Question No 03 /////////////////////// -->
    <!-- What are event listeners in the DOM? -->

    The addEventListener() method attaches an event handler to the specified element.
    The addEventListener() method attaches an event handler to an element without overwriting existing event handlers.
    You can add many event handlers to one element.

    Syntax: 
            element.addEventListener(event, function, useCapture);
    Example:
    element.addEventListener("click", function(){ alert("Hello World!"); });
    
</body>
</html>